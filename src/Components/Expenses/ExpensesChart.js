import React from "react";
import Chart from "../Chart/Chart";

const ExpensesChart = (props) => {
  // const chartDataPoints = [{ label, value }];

  return <Chart />;
};

export default ExpensesChart;
